[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc", "g++", "gfortran"]

[phases.install]
cmds = ["pip install --upgrade pip setuptools wheel", "pip install -r requirements.txt"]

[phases.start]
cmd = "cd backend && gunicorn app:app --bind 0.0.0.0:$PORT"

[variables]
PYTHON_VERSION = "3.11"